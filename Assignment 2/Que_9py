# Character Case Check : Write a program to input a character and check whether it is an uppercase letter,lowercase letter, or not a letter.
char = input()

if char.isupper():
  print(char, "is an uppercase letter.")
    # Check if the character is a lowercase letter
elif char.islower():
  print(char," is a lowercase letter.")
    # If neither, it's not a letter
else:
  print(char," is not a letter.")